{
    "title": "Domain Expiration Tracker Input",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "domains": {
            "title": "Domains",
            "type": "array",
            "editor": "stringList",
            "description": "Domains to check, without protocol or \"www.\" (e.g. \"example.com\"). One lookup is billed per domain. Not all TLDs support RDAP — mainstream gTLDs (.com, .net, .org, .info, .dev, .app, etc.) are well covered; some ccTLDs (e.g. \"co\", \"us\") are not.",
            "prefill": ["google.com"],
            "default": ["google.com"]
        }
    },
    "required": ["domains"]
}
